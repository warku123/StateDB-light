syntax = "proto3";

package sdbrpc;
option go_package="./sdbrpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Sdbrpc {
  rpc Ping(Request) returns(Response);
}
